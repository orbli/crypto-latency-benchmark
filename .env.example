# Development environment variables (copy to .env)
# WARNING: Never commit the actual .env file!

# Required for SBE and FIX streams
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_ed25519_private_key_base64_encoded

# Optional settings
USE_TESTNET=false
CONFIG_PATH=./config.yaml